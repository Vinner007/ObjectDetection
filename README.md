# 🎯 Object Detection - ตรวจจับวัตถุทุกอย่างบนโลก

> โปรแกรมตรวจจับวัตถุอัจฉริยะด้วย YOLOv8 และ YOLOv11 (State-of-the-Art) สามารถตรวจจับวัตถุได้ 80+ ประเภท แบบ Real-time

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/)
[![YOLOv8](https://img.shields.io/badge/YOLOv8-Ultralytics-00FFFF.svg)](https://github.com/ultralytics/ultralytics)
[![YOLOv11](https://img.shields.io/badge/YOLOv11-Latest-FF6B6B.svg)](https://github.com/ultralytics/ultralytics)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

**👨‍💻 Created by:** [Vinner](https://github.com/vinner)

---

## ✨ Features

- ✅ รองรับทั้ง **YOLOv8** และ **YOLOv11** (เวอร์ชันล่าสุด)
- 🎥 ตรวจจับแบบ Real-time จากกล้อง Webcam
- 📹 รองรับไฟล์วิดีโอทุกรูปแบบ
- 🖼️ ตรวจจับจากรูปภาพ
- 🎯 ตรวจจับวัตถุได้ 80+ ประเภท
- ⚡ ปรับโมเดลได้ตามความต้องการ (เร็ว/แม่น)
- 📊 แสดง FPS และจำนวนวัตถุแบบ Real-time
- 🎨 แสดงผลด้วย Bounding Box สีสันสวยงาม

## 📋 Requirements

- Python 3.8+
- OpenCV 4.8.0+
- Ultralytics 8.0.0+
- PyTorch 2.0.0+
- NumPy 1.24.0+

## 📦 การติดตั้ง

### วิธีที่ 1: ติดตั้งจาก requirements.txt (แนะนำ)
```bash
pip install -r requirements.txt
```

### วิธีที่ 2: ติดตั้งทีละตัว
```bash
pip install ultralytics opencv-python numpy torch
```

## 🚀 วิธีใช้งาน

### 🎮 YOLOv8 (Stable Version)

#### 1. ตรวจจับจากกล้อง (Webcam)
```bash
python object_detection.py
```

#### 2. ตรวจจับจากไฟล์วิดีโอ
```bash
python object_detection.py --source video.mp4
```

#### 3. ตรวจจับจากรูปภาพ
```bash
python object_detection.py --source image.jpg
```

#### 4. ใช้โมเดลที่แม่นยำกว่า
```bash
python object_detection.py --model yolov8m.pt
```

#### 5. ปรับค่าความมั่นใจ
```bash
python object_detection.py --conf 0.7
```

### 🔥 YOLOv11 (Latest - More Accurate & Faster)

#### 1. ตรวจจับจากกล้อง (Webcam)
```bash
python yolov11_detection.py
```

#### 2. ตรวจจับจากไฟล์วิดีโอ
```bash
python yolov11_detection.py --source video.mp4
```

#### 3. ใช้โมเดลที่แม่นที่สุด
```bash
python yolov11_detection.py --model yolo11x.pt
```

#### 4. ใช้โมเดลที่เร็วที่สุด
```bash
python yolov11_detection.py --model yolo11n.pt
```

## 🎮 Parameters

### พารามิเตอร์ทั่วไป
| Parameter | Description | Default | Example |
|-----------|-------------|---------|---------|
| `--source` | แหล่งที่มา (webcam/video/image) | `"0"` | `--source video.mp4` |
| `--model` | โมเดล YOLO ที่ต้องการใช้ | ดูตารางด้านล่าง | `--model yolov8m.pt` |
| `--conf` | ค่าความมั่นใจขั้นต่ำ (0-1) | `0.5` | `--conf 0.7` |

### 📊 YOLOv8 Models Comparison

| Model | Size | Speed | Accuracy | Parameters | Use Case |
|-------|------|-------|----------|------------|----------|
| `yolov8n.pt` | Nano | ⚡⚡⚡⚡⚡ | ⭐⭐⭐ | 3.2M | Real-time บนเครื่องธรรมดา |
| `yolov8s.pt` | Small | ⚡⚡⚡⚡ | ⭐⭐⭐⭐ | 11.2M | สมดุลระหว่างเร็วและแม่น |
| `yolov8m.pt` | Medium | ⚡⚡⚡ | ⭐⭐⭐⭐ | 25.9M | แม่นยำ, ใช้ CPU ได้ |
| `yolov8l.pt` | Large | ⚡⚡ | ⭐⭐⭐⭐⭐ | 43.7M | แม่นมาก, ต้องการ GPU |
| `yolov8x.pt` | XLarge | ⚡ | ⭐⭐⭐⭐⭐ | 68.2M | แม่นที่สุด, GPU แรงๆ |

### 🔥 YOLOv11 Models Comparison (Latest)

| Model | Size | Speed | Accuracy | Parameters | Use Case |
|-------|------|-------|----------|------------|----------|
| `yolo11n.pt` | Nano | ⚡⚡⚡⚡⚡ | ⭐⭐⭐⭐ | 2.6M | เร็วสุด, เบากว่า v8 |
| `yolo11s.pt` | Small | ⚡⚡⚡⚡ | ⭐⭐⭐⭐ | 9.4M | สมดุล, แนะนำ |
| `yolo11m.pt` | Medium | ⚡⚡⚡ | ⭐⭐⭐⭐⭐ | 20.1M | แม่นกว่า v8m |
| `yolo11l.pt` | Large | ⚡⚡ | ⭐⭐⭐⭐⭐ | 25.3M | แม่นมาก |
| `yolo11x.pt` | XLarge | ⚡ | ⭐⭐⭐⭐⭐ | 56.9M | State-of-the-Art 🏆 |

## 🏷️ วัตถุที่ตรวจจับได้ (80 classes)

คน, จักรยาน, รถยนต์, มอเตอร์ไซค์, เครื่องบิน, รถบัส, รถไฟ, รถบรรทุก, เรือ, สัญญาณไฟ, ป้ายจราจร, แมว, สุนัข, ม้า, แกะ, วัว, ช้าง, หมี, ม้าลาย, ยีราฟ, กระเป๋าเป้, ร่ม, กระเป๋าถือ, เนคไท, กระเป๋าเดินทาง, จานบิน, สกี, สโนว์บอร์ด, ลูกบอล, ว่าว, ไม้เบสบอล, ไม้เทนนิส, ขวด, แก้วไวน์, ถ้วย, ส้อม, มีด, ช้อน, ชาม, กล้วย, แอปเปิ้ล, แซนด์วิช, ส้ม, บรอกโคลี, แครอท, ฮอทด็อก, พิซซ่า, โดนัท, เค้ก, เก้าอี้, โซฟา, ต้นไม้, เตียง, โต๊ะอาหาร, ห้องน้ำ, ทีวี, แล็ปท็อป, เมาส์, รีโมท, คีย์บอร์ด, โทรศัพท์, ไมโครเวฟ, เตาอบ, โทสเตอร์, อ่างล้างจาน, ตู้เย็น, หนังสือ, นาฬิกา, แจกัน, กรรไกร, ตุ๊กตาหมี, ไดร์เป่าผม, แปรงสีฟัน และอื่นๆ

## 📁 Project Structure

```
ทท/
├── object_detection.py      # YOLOv8 detection script
├── yolov11_detection.py     # YOLOv11 detection script (Latest)
├── example_usage.py         # ตัวอย่างการใช้งาน
├── requirements.txt         # Python dependencies
├── yolov8n.pt              # Pre-trained YOLOv8 model
├── README.md               # คู่มือนี้
└── QUICKSTART.md           # คู่มือเริ่มต้นแบบรวดเร็ว
```

## ⌨️ ปุ่มควบคุม

- กด `q` เพื่อออกจากโปรแกรม

## 💡 Tips & Best Practices

### เลือกโมเดลอย่างไร?
- 💻 **คอมพ์ทั่วไป (CPU)**: ใช้ `yolov8n.pt` หรือ `yolo11n.pt`
- 🎮 **มี GPU ระดับกลาง**: ใช้ `yolov8m.pt` หรือ `yolo11m.pt`
- 🚀 **มี GPU แรงๆ**: ใช้ `yolov8x.pt` หรือ `yolo11x.pt`
- ⚡ **ต้องการเร็วที่สุด**: ใช้ `yolo11n.pt` (เร็วกว่า v8n)
- 🎯 **ต้องการแม่นที่สุด**: ใช้ `yolo11x.pt` (State-of-the-Art)

### ปรับค่า Confidence
- `--conf 0.3` = ตรวจจับได้มาก แต่อาจมีผลบวกปลอม (false positive)
- `--conf 0.5` = ค่ามาตรฐาน สมดุลดี (แนะนำ)
- `--conf 0.7` = ตรวจจับแต่วัตถุที่มั่นใจสูง ลด false positive

### Performance Tips
- ถ้าช้า: ลดขนาดวิดีโอหรือใช้โมเดลเล็กกว่า
- ถ้ารันครั้งแรกช้า: ปกติครับ กำลังดาวน์โหลดโมเดล
- ถ้าต้องการ FPS สูง: ใช้ YOLOv11n (เร็วสุด)

## 🚦 Quick Start

อ่าน [QUICKSTART.md](QUICKSTART.md) สำหรับคู่มือเริ่มต้นแบบรวดเร็ว

## 📖 Example Usage

ดูตัวอย่างเพิ่มเติมที่ [example_usage.py](example_usage.py)

## 🔧 Troubleshooting

### ปัญหาที่พบบ่อย

**Q: โปรแกรมไม่เจอกล้อง**
```bash
# ลองเปลี่ยนหมายเลขกล้อง
python object_detection.py --source 1
python object_detection.py --source 2
```

**Q: Error: No module named 'ultralytics'**
```bash
pip install ultralytics
```

**Q: CUDA out of memory (GPU)**
```bash
# ใช้โมเดลเล็กกว่า
python object_detection.py --model yolov8n.pt
```

**Q: ต้องการบันทึกวิดีโอผลลัพธ์**
```python
# ใช้ example_usage.py หรือแก้ไขโค้ดเพิ่ม VideoWriter
```

## 🤝 Contributing

มีไอเดียหรือพบบั๊ก? อยากปรับปรุง?
1. Fork โปรเจกต์นี้
2. สร้าง Branch ใหม่ (`git checkout -b feature/AmazingFeature`)
3. Commit การเปลี่ยนแปลง (`git commit -m 'Add some AmazingFeature'`)
4. Push ไปยัง Branch (`git push origin feature/AmazingFeature`)
5. เปิด Pull Request

## 📄 License

This project is licensed under the MIT License

## 🙏 Acknowledgments

- [Ultralytics](https://github.com/ultralytics/ultralytics) - YOLO implementation
- [OpenCV](https://opencv.org/) - Computer Vision library
- [PyTorch](https://pytorch.org/) - Deep Learning framework

---

<div align="center">

### 👨‍💻 Developed by **Vinner**

[![GitHub](https://img.shields.io/badge/GitHub-Vinner-181717?style=for-the-badge&logo=github)](https://github.com/vinner)
[![Email](https://img.shields.io/badge/Email-Contact-D14836?style=for-the-badge&logo=gmail)](mailto:vinner@example.com)

**⭐ ถ้าชอบโปรเจกต์นี้ อย่าลืมกด Star นะครับ! ⭐**

Made with ❤️ in Thailand 🇹🇭

</div>
