# ⚡ Quick Start Guide

## 🚀 เริ่มต้นใช้งานภายใน 1 นาที

### ขั้นตอนที่ 1: ติดตั้ง Dependencies
```bash
pip install -r requirements.txt
```

### ขั้นตอนที่ 2: รันโปรแกรม
```bash
python object_detection.py
```

**เท่านี้ก็เสร็จแล้ว!** โปรแกรมจะเปิดกล้องและเริ่มตรวจจับวัตถุอัตโนมัติ

---

## 📝 คำสั่งที่ใช้บ่อย

### ใช้กับวิดีโอ
```bash
python object_detection.py --source myvideo.mp4
```

### ใช้กับรูปภาพ
```bash
python object_detection.py --source myimage.jpg
```

### ใช้โมเดลที่แม่นกว่า
```bash
python object_detection.py --model yolov8m.pt
```

---

## ❓ FAQ

**Q: โปรแกรมไม่เจอกล้อง**
A: ลองเปลี่ยนเป็น `--source 1` หรือ `--source 2`

**Q: ตรวจจับได้ช้า**
A: ใช้โมเดล `yolov8n.pt` (เร็วสุด) หรือลดขนาดวิดีโอ

**Q: ต้องการแม่นยำมากขึ้น**
A: ใช้ `--model yolov8x.pt --conf 0.7`

**Q: รันครั้งแรกช้า**
A: ปกติครับ กำลังดาวน์โหลดโมเดล YOLO (ครั้งต่อไปจะเร็ว)

---

## 🎯 ตัวอย่างการใช้งานเพิ่มเติม

ดูได้ที่ [example_usage.py](example_usage.py)

---

## 🆘 ต้องการความช่วยเหลือ

อ่านคู่มือเต็มที่ [README.md](README.md)
